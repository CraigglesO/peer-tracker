{
  "name": "peer-tracker",
  "version": "0.0.0",
  "description": "Lightweight BitTorrent Tracker Client & Server Implementation",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "babel-node index.js",
    "build": "...",
    "git-commit": "git add -A . && git commit -a -m 'gh-pages update'",
    "git-push": "git push origin gh-pages --force && git checkout master",
    "deploy": "npm run build && npm run git-commit && npm run git-push"
  },
  "author": "Craig OConnor",
  "repository": {
    "type": "git",
    "url": "https://github.com/CraigglesO/peer-tracker.git"
  },
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0"
  },
  "dependencies": {
    "bittorrent-peerid": "^1.1.1",
    "compact2string": "^1.4.0",
    "mongoose": "^4.7.6",
    "string2compact": "^1.2.2",
    "ws": "^1.1.1"
  }
}
